import { Typography } from "@mui/material";
import { Box } from "@mui/system";

const Shop = (props) => {
    return (
        <Box>
            <Typography>Test</Typography>
        </Box>
    );
};

export default Shop;
